分析src\infrastructure\workflow\extensions目录提供的hook、plugins、trigger功能该如何集成。此外，分析如何实现配置驱动。节点、边、hook、触发器都需要支持一定程度的自定义配置(例如llm节点与提示词模块集成，允许设定所有参数；边允许自定义各类路由函数；工作流允许自由编排节点、边)。不过需要避免过度设计(例如工作流设置需要尽量使用预定义的子工作流，路由函数等只允许使用预定义的，只有条件可以自定义)。此外，需要考虑条件路由等功能如何实现。复杂编排先不考虑